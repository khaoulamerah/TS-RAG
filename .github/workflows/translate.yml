name: Translate Documentation

on:
  push:
    paths:
      - "docs/Fr/**"

jobs:
  translate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: echo "Workflow lancé !"